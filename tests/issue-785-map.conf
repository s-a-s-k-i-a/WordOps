worker_processes 1;
pid logs/nginx.pid;
error_log logs/error.log info;
events { worker_connections 128; }
http {
    include ../wo/cli/templates/map-wp.mustache;
    server {
        listen 127.0.0.1:18086;
        location / { return 200 "$skip_cache\n"; }
    }
}
